<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wordlist_name, "Wordlist Name" %>
    <%= f.text_field :wordlist_name %>
  </div>
  <div class="field">
    <%= f.label :starting_lives, "Starting Lives" %>
    <%= f.text_field :starting_lives %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
