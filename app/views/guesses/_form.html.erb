<%= form_for([@game, @game.guesses.build]) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this guess from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="guess">
    <%= f.label :guess %>
    <%= f.text_field :guess %>
  </div>

  <div class="actions">
    <%= f.submit "Submit guess" %>
  </div>
<% end %>
